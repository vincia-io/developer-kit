{
  "name": "connector-google-auth-provider",
  "version": "0.1.0",
  "description": "T1 auth-provider connector example: implements OAuthProvider for Google Sign-In.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@vincia/sdk": "https://get.vincia.io/cli/0.3.0/vincia-sdk-0.3.0.tgz"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
